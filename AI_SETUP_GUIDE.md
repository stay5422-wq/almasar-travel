# دليل إعداد الذكاء الاصطناعي
# AI Setup Guide

## نظرة عامة
تم دمج الذكاء الاصطناعي في التطبيق لأتمتة جميع مهام التسويق الرقمي.

---

## 1. تثبيت المكتبات المطلوبة

```bash
npm install openai
```

---

## 2. إعداد OpenAI API Key

### الحصول على API Key:

1. قم بزيارة: https://platform.openai.com
2. سجل الدخول أو أنشئ حساب جديد
3. اذهب إلى: **API Keys** في القائمة الجانبية
4. اضغط على **Create new secret key**
5. انسخ الـ API Key (احفظه في مكان آمن!)

### إضافة الـ API Key للتطبيق:

قم بفتح ملف `.env.local` وأضف:

```env
# OpenAI Configuration
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_MODEL=gpt-4-turbo-preview
```

---

## 3. ميزات الذكاء الاصطناعي المتاحة

### 🤖 1. توليد المحتوى التلقائي
- كتابة منشورات تلقائية لجميع المنصات
- دعم Facebook, Instagram, Twitter, LinkedIn, YouTube
- تخصيص النبرة واللغة والهاشتاجات

**API Endpoint:**
```
POST /api/ai/generate-content
```

**مثال:**
```json
{
  "clientName": "فندق قصر أبها",
  "clientType": "hotel",
  "platform": "facebook",
  "topic": "عروض الصيف",
  "tone": "professional",
  "language": "ar"
}
```

---

### 🎯 2. تحسين الحملات الإعلانية
- تحليل أداء الحملات الحالية
- توصيات لتحسين ROI
- اقتراحات للميزانية والجمهور المستهدف

**API Endpoint:**
```
POST /api/ai/optimize-campaign
```

**مثال:**
```json
{
  "campaignName": "حملة الصيف 2024",
  "platform": "facebook",
  "budget": 5000,
  "currentMetrics": {
    "impressions": 50000,
    "clicks": 1200,
    "conversions": 45
  }
}
```

---

### 📅 3. جدولة ذكية للمنشورات
- اقتراح أفضل أوقات النشر
- تحليل سلوك الجمهور
- توصيات بناءً على البيانات التاريخية

**API Endpoint:**
```
POST /api/ai/schedule
```

**مثال:**
```json
{
  "clientName": "شركة الماسر للسياحة",
  "platform": "instagram",
  "targetAudience": "سياح أبها",
  "contentType": "promotional"
}
```

---

### 🔍 4. تحليل SEO
- تحليل الكلمات المفتاحية
- استراتيجيات SEO مخصصة
- مراقبة المنافسين

**API Endpoint:**
```
POST /api/ai/seo-analysis
```

**مثال:**
```json
{
  "clientName": "فندق الماسر",
  "industry": "hospitality",
  "targetAudience": "السياح السعوديين والخليجيين",
  "competitors": ["فندق قصر الجبل", "فندق أبها الجديد"]
}
```

---

### 🎨 5. توليد الصور بالذكاء الاصطناعي
- إنشاء صور احترافية باستخدام DALL-E 3
- أنماط متعددة (احترافي، إبداعي، واقعي)
- أحجام مختلفة (1024x1024, 1024x1792, 1792x1024)

**API Endpoint:**
```
POST /api/ai/generate-image
```

**مثال:**
```json
{
  "prompt": "منظر جميل لجبال أبها مع غروب الشمس، تصوير احترافي",
  "style": "professional",
  "size": "1024x1024"
}
```

---

### 📊 6. تحليل المنافسين
- مقارنة الأداء مع المنافسين
- تحديد نقاط القوة والضعف
- استراتيجيات للتفوق

**API Endpoint:**
```
POST /api/ai/analyze-competitor
```

**مثال:**
```json
{
  "clientName": "فندق الماسر",
  "industry": "hospitality",
  "competitors": [
    {
      "name": "فندق قصر الجبل",
      "socialMedia": {
        "facebook": 50000,
        "instagram": 35000
      }
    }
  ]
}
```

---

### 📋 7. إنشاء استراتيجية المحتوى
- خطة محتوى شهرية كاملة
- جدول منشورات لجميع المنصات
- مواضيع وأفكار مبتكرة

**API Endpoint:**
```
POST /api/ai/content-strategy
```

**مثال:**
```json
{
  "clientName": "فندق قصر أبها",
  "clientType": "hotel",
  "platforms": ["facebook", "instagram", "twitter"],
  "goals": ["زيادة الوعي بالعلامة التجارية", "زيادة الحجوزات"],
  "duration": "30"
}
```

---

### 💬 8. مساعد AI محادثة
- الإجابة على أسئلة التسويق
- نصائح وتوصيات فورية
- دعم متعدد اللغات

**API Endpoint:**
```
POST /api/ai/chat
```

**مثال:**
```json
{
  "message": "ما هي أفضل استراتيجية لزيادة التفاعل على Instagram؟",
  "context": {
    "clientType": "hotel",
    "platform": "instagram"
  }
}
```

---

### 📈 9. توليد التقارير التلقائية
- تقارير أداء شاملة
- رسوم بيانية وإحصائيات
- توصيات وخطط عمل

**API Endpoint:**
```
POST /api/ai/generate-report
```

**مثال:**
```json
{
  "clientName": "فندق الماسر",
  "reportType": "monthly",
  "startDate": "2024-01-01",
  "endDate": "2024-01-31",
  "metrics": {
    "posts": 45,
    "engagement": 12500,
    "reach": 250000
  }
}
```

---

## 4. استخدام واجهة AI Assistant

### الوصول للصفحة:
```
http://localhost:3001/ar/ai-assistant
```

### الميزات المتاحة في الواجهة:

1. **Dashboard الذكي**: إحصائيات شاملة عن استخدام AI
2. **إجراءات سريعة**: أزرار مباشرة لأهم المهام
3. **بطاقات الميزات**: الوصول لجميع قدرات AI
4. **المهام الأخيرة**: متابعة العمليات المكتملة
5. **واجهة الدردشة**: تواصل مباشر مع المساعد الذكي

---

## 5. دمج AI في الصفحات الموجودة

### صفحة المحتوى (Content):
- زر "إنشاء بالذكاء الاصطناعي" لكتابة منشورات تلقائية
- اقتراحات للعناوين والوصف

### صفحة شراء الإعلانات (Media Buying):
- زر "تحسين الحملة" لتحليل الأداء
- توصيات لتحسين ROI

### صفحة SEO:
- زر "تحليل بالذكاء الاصطناعي" للكلمات المفتاحية
- اقتراحات استراتيجيات SEO

### صفحة التحليلات (Analytics):
- زر "توليد تقرير" لإنشاء تقارير شاملة
- رؤى ذكية للبيانات

---

## 6. الأسعار والحدود

### OpenAI Pricing (تقريبي):

- **GPT-4 Turbo**:
  - Input: $10 / 1M tokens
  - Output: $30 / 1M tokens
  
- **DALL-E 3**:
  - 1024x1024: $0.040 / صورة
  - 1024x1792: $0.080 / صورة

### نصائح لتوفير التكاليف:

1. استخدم GPT-3.5-turbo للمهام البسيطة
2. قم بتخزين النتائج المكررة (caching)
3. حدد max_tokens لتقليل الاستهلاك
4. استخدم streaming للردود الطويلة

---

## 7. الأمان وأفضل الممارسات

### ✅ افعل:
- احفظ API Key في ملف `.env.local` فقط
- لا تشارك API Key مع أحد
- راقب الاستخدام من لوحة تحكم OpenAI
- ضع حدود للطلبات (rate limiting)

### ❌ لا تفعل:
- لا تضع API Key في الكود مباشرة
- لا ترفع `.env.local` إلى GitHub
- لا تشارك API Key في رسائل أو emails
- لا تستخدم نفس API Key في بيئات مختلفة

---

## 8. اختبار الميزات

### اختبار توليد المحتوى:
```bash
curl -X POST http://localhost:3001/api/ai/generate-content \
  -H "Content-Type: application/json" \
  -d '{
    "clientName": "فندق قصر أبها",
    "clientType": "hotel",
    "platform": "facebook",
    "topic": "عروض الصيف"
  }'
```

### اختبار الدردشة:
```bash
curl -X POST http://localhost:3001/api/ai/chat \
  -H "Content-Type: application/json" \
  -d '{
    "message": "كيف أحسن تفاعل منشوراتي؟"
  }'
```

---

## 9. استكشاف الأخطاء

### مشكلة: "API key not found"
**الحل:**
- تأكد من وجود `OPENAI_API_KEY` في `.env.local`
- أعد تشغيل السيرفر بعد تعديل `.env.local`

### مشكلة: "Rate limit exceeded"
**الحل:**
- انتظر دقيقة وأعد المحاولة
- تحقق من حد الاستخدام في OpenAI Dashboard

### مشكلة: "Invalid request error"
**الحل:**
- تحقق من صيغة الـ JSON المرسل
- تأكد من وجود جميع الحقول المطلوبة

---

## 10. الدعم والمساعدة

### الموارد:
- [OpenAI Documentation](https://platform.openai.com/docs)
- [OpenAI API Reference](https://platform.openai.com/docs/api-reference)
- [OpenAI Cookbook](https://cookbook.openai.com/)

### الحصول على المساعدة:
- [OpenAI Community Forum](https://community.openai.com)
- [OpenAI Discord](https://discord.gg/openai)

---

## 🎉 جاهز للبدء!

بعد إتمام هذه الخطوات، سيكون لديك:

✅ نظام ذكاء اصطناعي كامل
✅ 9 ميزات قوية للأتمتة
✅ واجهة سهلة الاستخدام
✅ توفير الوقت والجهد

**الآن يمكن للذكاء الاصطناعي أن يقوم بكل شيء نيابة عنك! 🚀**
